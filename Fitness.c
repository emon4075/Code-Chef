// https://www.codechef.com/problems/FIT
#include <stdio.h>

int main(void)
{
    int T, X;
    scanf("%d", &T);
    while (T--)
    {
        scanf("%d", &X);
        printf("%d\n", (X * 2 * 5));
    }

    return 0;
}
